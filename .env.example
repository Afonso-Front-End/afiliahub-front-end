# Produção: URL pública da API (ex.: https://api.seudominio.com)
# Desenvolvimento: pode ficar vazio — o Vite faz proxy de /api para localhost:4000
VITE_API_URL=

# Backend: copie backend/.env.example para backend/.env e preencha
