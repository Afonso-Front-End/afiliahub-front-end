# URL pública da API em produção (obrigatório na Vercel)
# Ex.: https://api.seudominio.com
VITE_API_URL=

# Desenvolvimento local: deixe vazio — o Vite faz proxy de /api e /uploads para localhost:4000
# Backend local: copie backend/.env.example para backend/.env
